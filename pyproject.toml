[build-system]
requires = ["maturin>=0.14,<0.15"] 
build-backend = "maturin" 

[project]
name = "builder" 
description = "Build project." 
requires-python = ">=3.10" 

[tool.maturin]
bindings = "bin" 
strip = true 
